# Generated lockfile — managed by pnpm, not prettier.
pnpm-lock.yaml

# Vendored OnlyOffice SDK assets (sdkjs/web-apps/fonts and their manifests) —
# huge minified bundles that crash prettier's parser (V8 stack overflow), not
# hand-written. Mirrors the oxlint ignores; see .oxlintrc.json.
public/sdkjs/
public/web-apps/
public/fonts/
public/plugins.json
public/themes.json
public/document_editor_service_worker.js

# Vendored ranui design-token layer (copied from ranui/dist by bin/build.sh).
public/ran-tokens.css

# Vendored ranui per-component bundles (official dist/iife files, copied from
# ranui by bin/build.sh) — generated and minified, not hand-written.
public/ranui-iife/

# Generated Traditional-Chinese editor bundles (built by bin/build-zh-tw.js from
# the stock app.js via opencc-js). Huge generated files, not hand-written.
public/web-apps/apps/*/main/app.zh-tw.js
